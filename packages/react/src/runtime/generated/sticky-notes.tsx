import { StickyNotesSource as source } from '@rough-lucide/icons/source/sticky-notes';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const StickyNotes = createRuntimeIcon(source);
