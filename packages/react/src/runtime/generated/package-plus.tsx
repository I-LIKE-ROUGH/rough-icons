import { PackagePlusSource as source } from '@rough-lucide/icons/source/package-plus';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const PackagePlus = createRuntimeIcon(source);
