import { CloudUploadSource as source } from '@rough-lucide/icons/source/cloud-upload';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const CloudUpload = createRuntimeIcon(source);
