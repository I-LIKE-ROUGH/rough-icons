import { GlobeLockSource as source } from '@rough-lucide/icons/source/globe-lock';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const GlobeLock = createRuntimeIcon(source);
