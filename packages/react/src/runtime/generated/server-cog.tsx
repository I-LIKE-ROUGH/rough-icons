import { ServerCogSource as source } from '@rough-lucide/icons/source/server-cog';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const ServerCog = createRuntimeIcon(source);
