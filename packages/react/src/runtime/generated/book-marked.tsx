import { BookMarkedSource as source } from '@rough-lucide/icons/source/book-marked';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const BookMarked = createRuntimeIcon(source);
