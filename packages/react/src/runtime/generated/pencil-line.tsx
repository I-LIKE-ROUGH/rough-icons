import { PencilLineSource as source } from '@rough-lucide/icons/source/pencil-line';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const PencilLine = createRuntimeIcon(source);
