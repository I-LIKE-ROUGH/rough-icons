import { BadgeCheckSource as source } from '@rough-lucide/icons/source/badge-check';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const BadgeCheck = createRuntimeIcon(source);
