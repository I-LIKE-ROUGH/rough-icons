import { AppWindowSource as source } from '@rough-lucide/icons/source/app-window';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const AppWindow = createRuntimeIcon(source);
