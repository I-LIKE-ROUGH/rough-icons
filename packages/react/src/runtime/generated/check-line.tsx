import { CheckLineSource as source } from '@rough-lucide/icons/source/check-line';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const CheckLine = createRuntimeIcon(source);
