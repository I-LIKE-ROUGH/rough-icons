import { ServerCrashSource as source } from '@rough-lucide/icons/source/server-crash';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const ServerCrash = createRuntimeIcon(source);
