import { ListRestartSource as source } from '@rough-lucide/icons/source/list-restart';
import { createRuntimeIcon } from '../../create-runtime-icon.js';
export const ListRestart = createRuntimeIcon(source);
